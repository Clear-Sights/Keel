__pycache__/
*.py[cod]
.pytest_cache/
# coqc build output; tools/check_coq.py recompiles from source every run
proofs/*.vo
proofs/*.vos
proofs/*.vok
proofs/*.glob
proofs/.*.aux
# Agent worktrees: `git worktree` checkouts the harness creates inside the repo.
# Scratch, like the coqc output above -- never project content.
.claude/worktrees/
