# L9 CONFIRMED [loader/weakness] plugin/keel/clauses.py _base_predicate() l.230-238 (early `return any(...)` precedes the `event`/`tools` checks); plugin/keel/dispatch.py pre_tool_use l.401 and post_tool_use l.464, which call discharges() unfiltered

## claim
A predicate carrying any_of/all_of and no kind is a composition of whole predicates 'each evaluated on its own terms'; a side's declared `event`/`tools` bound which surface it reads.

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack
python3 $S/REPRO.py     # section [L8]
# q = {"event":"PostToolUse","tools":["Bash"],
#      "any_of":[{"kind":"nonzero","on":"tool_response.exit_code"},{"kind":"effect","effect":"report_paths"}]}
# evaluated on {"hook_event_name":"SessionStart","tool_name":"Read","keel_effect":{"report_paths":True}}
# and the shipped-table audit:
python3 -c "import json,sys; sys.path.insert(0,'/home/user/Keel/plugin'); from keel import clauses as C; rows=json.load(open('/home/user/Keel/plugin/keel/clauses.json')); print([(r['id'],s) for r in rows for s in ('fingerprint','activated_by','discharged_by') if isinstance(r.get(s),dict) and (r[s].get('any_of') or r[s].get('all_of')) and r[s].get('kind') is None])"

## observed
_base_predicate returns True for a guard that declares event=PostToolUse, tools=[Bash] when handed a SessionStart/Read event: the declared surface is silently dropped. The loader admits such a row without comment. Audit of the shipped table: 9 composed/branching sides (A02, U03, U09, U10, U12, U13, U19, U20 x2) all happen to leave the top-level event/tools unset, so this is a live trap for the next author rather than a currently-exploited hole -- and the fixture laws cannot catch it either, since _fixture_event builds the event from the same ignored declaration.
