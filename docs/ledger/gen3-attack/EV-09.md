# EV-09 CONFIRMED [evidence/weakness] tests/test_fence.py:192 `self.assertTrue(C_MOD.CONSTRUCTION_ANCHOR.fullmatch(anchor), ...)` in `test_every_construction_anchor_resolves`; plugin/keel/clauses.py:23; plugin/keel/dispatch.py:231

## claim
test_fence:186-192 -- "The shape assertion the loader gave up, held against the pattern the loader still owns ... `fullmatch`, not `assertRegex`: the pattern carries no anchors of its own, so a search would accept `see POINTS.md#a01 for details` as an anchor. The loader used fullmatch; moving the check must not weaken it."

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack; cp -r /home/user/Keel $S/mut_anchor; cd $S/mut_anchor
python3 - <<'EOF'
import pathlib
p=pathlib.Path("plugin/keel/clauses.py"); t=p.read_text()
old='CONSTRUCTION_ANCHOR = re.compile(r"POINTS\\.md#[a-z0-9][a-z0-9-]*")'
assert t.count(old)==1
p.write_text(t.replace(old,'CONSTRUCTION_ANCHOR = re.compile(r".*")',1))
EOF
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=plugin python3 -m unittest discover -s tests 2>&1 | tail -3

## observed
`Ran 246 tests in 326.490s ... OK`, EXIT=0. Widening the anchor pattern to `.*` makes the fence's shape assertion vacuous (it fullmatches everything) AND simultaneously disables the production filter at `dispatch._construction` (`if not C.CONSTRUCTION_ANCHOR.fullmatch(anchor): return ""`), whose whole job is to keep a misshapen pointer out of a deny message -- one edit, two safeguards off, zero red. The pattern is defined in exactly one place and asserted against itself; nothing pins the pattern's own text.
