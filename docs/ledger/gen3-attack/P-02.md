# P-02 NOVERDICT [points/break] A01, A02, A03 (tools:[Bash]), U12, U13, U19, U20 (PostToolUse tools:[Bash])

## claim
'Every occasion reads the world, not the program's name'; U12/U13/U19 fire on 'a file that existed before the act has different content after it — rewritten by whatever program'; U20 on destruction, 'the pre-image is retained'.

## reproducer
bash /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work/setup.sh && cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && python3 repro.py write
# and the manifest: cat /home/user/Keel/plugin/hooks/hooks.json | python3 -c "import json,sys;h=json.load(sys.stdin)['hooks'];print('PostToolUse matcher:',h['PostToolUse'][0]['matcher']);print('PreToolUse matcher:',h['PreToolUse'][0]['matcher'])"

## observed
f.txt (content 'hello\n') was truncated to '' through the host Write tool and then rewritten through Edit. Zero demands were raised — obligations.jsonl was never even created ('ledger exists: False') — and Stop returned ALLOW. No snapshot is taken for a non-Bash PreToolUse (dispatch.py `_effect_record` snapshots only when tool_name=='Bash'), so the retained-pre-image recovery story does not hold on this path either. In a real install the hook is not even invoked: PostToolUse matcher is 'Bash|Read' and PreToolUse is 'Agent|AskUserQuestion|Bash|ExitPlanMode|Glob|Grep|Read|Task'. Write, Edit, MultiEdit and NotebookEdit are outside all 24 occasions.
