# P-26 NOVERDICT [points/weakness] T02

## claim
T02 is listed as a shipped clause with a costly fate ('end the run treating a push report as a landing') and a guard the session must satisfy.

## reproducer
cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && bash setup.sh && python3 repro.py t02 && python3 -c "import json;d={c['id']:c for c in json.load(open('/home/user/Keel/plugin/keel/clauses.json'))};print(json.dumps(d['T02']['discharged_by']))"

## observed
T02.discharged_by is {"kind":"effect","effect":"remote_landed"}, and remote_landed is computed by Keel itself in effects.at_stop during reconcile — the agent performs no act to discharge it. In the named-push run T02 activated and Stop returned ALLOW with no guard call of any kind. So T02 is a verification step Keel runs, not a demand the table prices; its 'guard' column in the README ('let the ending measure the remote') describes work Keel does. Combined with P-16 (activation is evaded by pushing by URL), the only clause that actually checks whether a publish landed is both unrequested and optional.
