# EV-02 CONFIRMED [evidence/break] README.md:113 `the seven acts[^m-act-count]`; tests/test_fence.py:43 `PAGES = (SKILL, POINTS_MD, ACTS_MD)`; MEASURED.tsv row `act-count`

## claim
README line 113 says "the seven acts" and footnotes it to `[^m-act-count]: The act headings in plugin/ACTS.md, counted by the act-count command in MEASURED.tsv`. tests/test_fence.SpelledCountsMatchWhatTheyCount exists precisely because "raising ACTS.md from seven acts to ten ... left SKILL.md's index entry reading 'the seven coarser acts', and the whole fence stayed green."

## reproducer
cd /home/user/Keel && sed -n '113p;361p' README.md && python3 -c 'import pathlib,re; print(len(re.findall(r"^## ", pathlib.Path("plugin/ACTS.md").read_text(), re.M)))' && awk -F'\t' '$1=="act-count"{print $2}' MEASURED.tsv && grep -n '^PAGES' tests/test_fence.py

## observed
README:113 = "the seven acts[^m-act-count]"; the footnote's own command prints `10`; MEASURED.tsv act-count VALUE = `10`; `PAGES = (SKILL, POINTS_MD, ACTS_MD)` -- README.md is not swept. The exact drift the class was written against survives, one document over, in the most-read file, and the full 246-test suite is green (`Ran 246 tests ... OK`).
