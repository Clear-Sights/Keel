# P-04 NOVERDICT [points/break] U06 vs U24

## claim
U06 fires on 'send a mutating request to an external service'; U24 on 'publish or release an artifact after runtime testing'.

## reproducer
python3 -c "import json;d={c['id']:c for c in json.load(open('/home/user/Keel/plugin/keel/clauses.json'))};print('U06 fp',d['U06']['fingerprint']);print('U24 fp',d['U24']['fingerprint'])" && cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && bash setup.sh && python3 repro.py u03c09

## observed
Both fingerprints are exactly {"kind":"effect","effect":"net_out"}. Live, a single `nohup sleep 3 &` (which sends nothing anywhere) raised U06 and U24 together, along with U01. The table cannot tell a publish from a mutating request from a host that opened a socket; a NOT-EVALUABLE net counter (the documented case) raises both as real demands, not as NOT-EVALUABLE.
