# DL-11 CONFIRMED [dispatch/break] effects.write_observed / _write remote.json -> `state / OBSERVED`, `state / REMOTE` (one file per state dir, not per session); effects._artifact_read (compares the PATH only, never the document's `root` against the event's cwd); clauses A01/A02/A03/T01

## claim
A01: "Read Keel's worktree measurement, observed.json ...: the branch, HEAD and dirty paths Keel measured before the act"; A03: "so a force-push under any name cannot overwrite a tip that was never seen".

## reproducer
cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad && rm -rf st17 wA wB && mkdir -p wA wB
for d in wA wB; do (cd $d && git init -q . && git config user.email a@b.c && git config user.name a && echo "$d file" > only-in-$d.txt && git add -A && git commit -qm $d); done
P=$PWD/attack/dispatch/plugin; ST=$PWD/st17
h(){ printf '%s' "$1" | KEEL_STATE_DIR=$ST CLAUDE_PLUGIN_ROOT=$P bash $P/hooks/dispatch.sh >/dev/null; }
h "{\"session_id\":\"A\",\"hook_event_name\":\"SessionStart\",\"cwd\":\"$PWD/wA\"}"; cat $ST/observed.json
h "{\"session_id\":\"B\",\"hook_event_name\":\"SessionStart\",\"cwd\":\"$PWD/wB\"}"; cat $ST/observed.json
printf '%s' "{\"session_id\":\"A\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Read\",\"tool_input\":{\"file_path\":\"$ST/observed.json\"},\"tool_response\":{},\"cwd\":\"$PWD/wA\"}" | KEEL_STATE_DIR=$ST CLAUDE_PLUGIN_ROOT=$P bash $P/hooks/dispatch.sh
printf '%s' "{\"session_id\":\"A\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git push --force\"},\"cwd\":\"$PWD/wA\"}" | KEEL_STATE_DIR=$ST CLAUDE_PLUGIN_ROOT=$P bash $P/hooks/dispatch.sh

## observed
After session A's SessionStart, observed.json has "root": ".../wA", head 821508f4...  After session B's SessionStart in a different repository, the SAME file now reads "root": ".../wB", head c86c813a...  Session A then Reads that file and its A01/A02 (and T01) are discharged for the rest of its session, and the subsequent `git push --force` is denied only for [A03].
The datum A01/A02/T01 are paid by is a single shared file with no session or repository scoping, and _artifact_read never compares the document's `root` to the event's `cwd`. Two concurrent Claude Code windows (or a subagent started in another directory) are enough: the "observation of the worktree" that licenses a force-push in repo A is a measurement of repo B. On the coverage question, the answer is also positive in one direction -- a Read of a mere look-alike copy at another path does NOT pay (_artifact_read resolves and compares paths, verified: copy->False, symlink to the real file->True, `./` form->True, relative path->False) -- but a hard link to the identical inode also returns False, so the identity test is path-based and inconsistent.
