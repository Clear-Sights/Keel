# L4 CONFIRMED [loader/break] plugin/keel/clauses.py _leaves() (l.543-547) and _compile() effect check (l.488-491, CLAUSE-EFFECT-UNKNOWN); same blindness in _predicate()'s unmeasured scan (l.280) and in tools/render_coverings.py::_leaves

## claim
A clause naming an effect the observer does not measure is refused CLAUSE-EFFECT-UNKNOWN; class `effect` means the side reads what the act DID, named in keel.effects.EFFECTS (Theorem 8).

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack
python3 $S/REPRO.py     # section [L2]
# clause Z02: fingerprint   {"kind":"effect","effect":"totally_made_up_effect",
#                            "any_of":[{"kind":"effect","effect":"files_changed"}]}
#             discharged_by {"kind":"effect","effect":"another_fake_effect",
#                            "any_of":[{"kind":"effect","effect":"report_paths"}]}
# end to end (table = Z02 only):
python3 $S/z02run.py

## observed
ADMITTED -- no CLAUSE-EFFECT-UNKNOWN -- and reported as `fp=effect/world guard=effect/world`, i.e. "the observer measures it". `match()` on an event carrying every real effect set True returns False: the row is a permanent no-op that the loader, the class census and the generated Coq instance all certify as a world-observed covering. Through the hook the clause never denies and never discharges anything. (Against the immediately-preceding revision of _predicate, which read the TOP predicate's kind, the same admitted row instead made every PostToolUse NOT-EVALUABLE -> fail-closed and undischargeable; the escape is the same, only its blast radius moved.)
