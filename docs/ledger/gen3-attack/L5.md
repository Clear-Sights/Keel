# L5 CONFIRMED [loader/break] plugin/keel/clauses.py classify_side l.399-400 (`if kind in ("program","pipeline"): return "nominal"`), _admit CLAUSE-OCCASION-NOMINAL l.589-593 and CLAUSE-GUARD-NOMINAL l.600-604, _base_predicate l.240-263

## claim
The loader refuses a nominal occasion and a nominal guard outright; these two codes are what makes 'every occasion and guard is name-agnostic' an enforced law rather than a convention.

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack
cp -r /home/user/Keel $S/nonom      # then splice out both nominal `raise ClauseError(...)` blocks
# with the law in place:
python3 - <<'EOF'
from plant import plant           # plants a kind:"program" fingerprint (id Z11)
EOF
# with both raises replaced by `pass` (script in transcript), load the SAME rows:
#   - Z11 (kind:"program" occasion, fixtures_pos ["git push origin main"])
#   - the exact row tests/test_derived_closure.py::test_TEETH_a_nominal_guard_is_refused plants on U03

## observed
With the law in place: REFUSED CLAUSE-OCCASION-NOMINAL. With BOTH nominal raises deleted: Z11 is still refused, as CLAUSE-FIXTURE-POS-MISS; the U03 nominal-guard row is still refused, as CLAUSE-GUARD-FIXTURE-MISS. `_base_predicate({'kind':'program',...}, <matching event>)` = False. No table's admissibility changes when the two headline laws are removed: they can only refuse shapes that the fixture laws already refuse, because the only shapes they recognise are two kinds the evaluator cannot evaluate. Meanwhile every predicate that genuinely selects on a spelling (L1, L2, L3) is admitted.
