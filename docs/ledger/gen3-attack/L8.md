# L8 CONFIRMED [loader/weakness] plugin/keel/clauses.py _matches_a_tool_enum() (l.338-350, defined and never called) and derive_closure() (l.413-427, `composed -> host`)

## claim
'A clause may say it composed; this asks the table whether it did.' `composed` = a composition of the agnostic classes, every branch agnostic; closure `host` = a closed host enum covers the act.

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack
grep -rn '_matches_a_tool_enum' /home/user/Keel --include='*.py' --include='*.v' --include='*.md'
python3 $S/REPRO.py     # section [L7]
# predicate: {"any_of":[{"kind":"nonzero","on":"tool_response.exit_code"},
#                       {"kind":"effect","effect":"report_paths"}]}

## observed
grep returns only the definition (clauses.py:338) and its own recursive call (clauses.py:349) -- zero call sites in the loader, the dispatcher, the tests, the proof renderer. classify_side = 'composed', derive_closure = 'host', while `_matches_a_tool_enum(p)` = False: the closure asserts a closed host enum covers the act for a predicate in which no branch reads tool_name. tools/render_coverings.py stamps `class=composed closure=host` into proofs/Clauses.v on that basis.
