# EV-01 CONFIRMED [evidence/break] eval/replay.py, eval/corpus/*.jsonl, .github/workflows/release.yml ("Run the replay evidence"), MEASURED.tsv row `replay`, README "## Evidence"

## claim
README: "the corpus replay below proves the dispatcher denies at or before the derailment event in every authored fixture"; release.yml gates the release bundle on `python3 eval/replay.py` alone and advertises "Verified at release time: the 26/26 corpus replay"; MEASURED.tsv publishes replay 26/26.

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack; mkdir -p $S; cp -r /home/user/Keel $S/effgut3; cd $S/effgut3; python3 - <<'EOF'
import pathlib
p=pathlib.Path("plugin/keel/effects.py"); t=p.read_text()
a='def report_effects(stdout: Any, command: Any) -> dict[str, bool]:'
b='def trace_effects(text: str, before: dict[str, Any], root: str | None, quiet: bool) -> dict[str, Any]:'
c='def delta(state: pathlib.Path, session: str, agent: str, event: dict[str, Any]) -> dict[str, Any]:'
t=t.replace(a,a+'\n    return {"report_null": False, "report_pass": False, "report_clean": False, "report_fail": False, "report_nowarn": False, "report_signature": False, "report_structured": False, "report_self": False}',1)
t=t.replace(b,b+'\n    return {"report_ref": False, "report_paths": False, "report_pids": False, "report_listing": False}',1)
t=t.replace(c,c+'\n    return {name: None for name in EFFECTS}',1)
p.write_text(t)
EOF
python3 eval/replay.py | tail -2; echo "exit=$?"; python3 eval/generate_corpus.py --check
python3 -c 'import re,subprocess; p=subprocess.run(["python3","eval/replay.py"],capture_output=True,text=True); m=re.search(r"REPLAY sessions=(\\d+) passed=(\\d+) failed=0",p.stdout); print(m.group(2) if p.returncode==0 and m else "FAIL")'

## observed
`REPLAY sessions=26 passed=26 failed=0`, exit=0; `eval/corpus matches 26 specs`; the MEASURED `replay` row's own command still prints `26`. The whole effect-observation layer -- every `report_*` reading and the entire Bash effect delta -- returns constant blind values and the release gate is fully green. Cause: `dispatch._effect_record` starts with `if isinstance(event.get("keel_effect"), dict): return`, and `eval/generate_corpus.py` writes `full(**eff)` (a complete authored record) into every PostToolUse event, so 25 of 26 sessions never call `effects.delta`/`report_effects`/`trace_effects` at all. Only the unittest suite catches this (24 failures, 3 errors), and neither the CI "Corpus replay" step nor release.yml runs the suite before cutting the bundle.
