# P-25 NOVERDICT [points/weakness] U06, U24

## claim
README: 'the host's connection counter has no lineage and is assigned by the idle gap — if it moved while no act of this session was running, its movement across the act is NOT-EVALUABLE, never the act's ... on a host that opens connections by itself they are raised once as NOT-EVALUABLE.'

## reproducer
cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && bash setup.sh && python3 repro.py u03c09 2>&1 | grep -A2 'demand rows' | head

## observed
`nohup sleep 3 &` and `git commit -qS` (repro.py u08) each produced demands U06 and U24 in a sandbox whose proxy opens connections continuously. NOT-EVALUABLE is not surfaced as a distinct outcome to the operator: dispatch treats None as the occasion being live, so both clauses raise ordinary demands with ordinary deny reasons ('an outbound connection was made with no network read on record', 'a publish reaches its registry through one'). On any host with background network activity, every act carries a publish obligation and a mutating-request obligation — which, combined with P-08 (echo PASS pays U24) and P-10 (U06 self-licenses on real traffic), inverts the clause: it fires on the acts that sent nothing and stays silent on the ones that did.
