# DL-02 NOVERDICT [dispatch/break] clauses.json A02.discharged_by any_of[{kind:regex,on:tool_name,pattern:^Glob$}]; dispatch.pre_tool_use guard branch; hooks.json PostToolUse matcher "Bash|Read"

## claim
A02: "list the set first: the host Glob tool ... so a later loss has a record" -- and README/owner's law that a guard is discharged by an OBSERVED EFFECT of the act.

## reproducer
cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad && rm -rf st3
P=$PWD/attack/dispatch/plugin; ST=$PWD/st3
h(){ printf '%s' "$1" | KEEL_STATE_DIR=$ST CLAUDE_PLUGIN_ROOT=$P bash $P/hooks/dispatch.sh; echo; }
h '{"session_id":"s3","hook_event_name":"SessionStart","cwd":"/tmp"}' >/dev/null
h '{"session_id":"s3","hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"git push"}}'
h '{"session_id":"s3","hook_event_name":"PreToolUse","tool_name":"Glob","tool_input":{"pattern":"/nonexistent/**/*.nope"}}'
h '{"session_id":"s3","hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"git push"}}'

## observed
First Bash: deny naming [A01] [A02] [A03]. The Glob PreToolUse for a pattern under a directory that does not exist: allowed, and the ledger gains `discharge ... "guard call observed"`. Second Bash: deny naming only [A01] [A03] -- A02 is paid.
The listing was never performed (this is the pre-tool event) and can never be checked: hooks.json registers no PostToolUse for Glob, so no observed effect of a Glob ever reaches the dispatcher. "Nothing listed a set first" is discharged by an intention to list nothing. A Grep, by contrast, pays nothing -- so "a sequence of host Read/Grep/Glob calls" pays A02/D01/P01/C03 and never pays A01/A03 (those need the two artifact Reads).
