# DL-15 CONFIRMED [dispatch/weakness] dispatch._effect_record("before") vs ("after"); pre_tool_use's guard branch never sees a Read's keel_effect (read_delta is only computed at PostToolUse)

## claim
README's COMPOSING property: "discharging one link surfaces what remains, and no entry point skips a predecessor"; the deny message's instruction "Read `observed.json` ... first".

## reproducer
cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad && rm -rf st16
python3 - "$PWD/st16" "$PWD/w1" <<'PY'
import sys
sys.path.insert(0,'/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad')
exec(open('/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/drive.py').read())
ST,W=sys.argv[1],sys.argv[2]; S="s16"
def ev(n,t=None,**kw):
    e={"session_id":S,"hook_event_name":n,"cwd":W}
    if t: e["tool_name"]=t
    e.update(kw); return e
go(ev("SessionStart"))
go(ev("PreToolUse","Read",tool_input={"file_path":ST+"/observed.json"}))
go(ev("PreToolUse","Read",tool_input={"file_path":ST+"/remote.json"}))
go(ev("PreToolUse","Bash",tool_input={"command":"git push"}))
go(ev("PostToolUse","Read",tool_input={"file_path":ST+"/observed.json"},tool_response={}))
go(ev("PostToolUse","Read",tool_input={"file_path":ST+"/remote.json"},tool_response={}))
PY

## observed
The turn batches [Read observed.json, Read remote.json, Bash git push]. Host order Pre(R1), Pre(R2), Pre(Bash): both guard Reads were already issued and admitted, and the Bash is still DENIED naming [A01][A02][A03], because a Read's `observed_read`/`remote_read` effect is only computed at PostToolUse -- which arrives after. The refusal then writes the three demand rows, which (per DL-14) go on to deny every later non-read call.
So the answer to the ordering question is: PreToolUse deny always precedes PostToolUse discharge, and the dispatcher has no way to see a guard that is in flight. Within a single Pre-block this is a guaranteed false refusal for the exact sequence the deny message asks the agent to perform, and it is not recoverable by writing the calls in the right order -- only by spending a whole extra turn.
