# P-16 CONFIRMED [points/break] T02

## claim
Occasion: 'A remote ref moved during this session — by whatever program — and the run is ending.' Activation predicate is the effect `remote_ref_moved`, defined as 'a remote-TRACKING ref names a different commit after the act'.

## reproducer
bash /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work/setup.sh && cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && python3 repro.py t02

## observed
`git push origin master` -> demand rows [('T02','activated')], i.e. the ending will measure the remote. `git push <bare-repo-URL> HEAD:refs/heads/master` — the same commits reaching the same remote — -> demand rows [] : T02 never activates, and Stop returns ALLOW without listing the remote. Pushing by URL, by a refspec with no tracking configuration, or through an API client (gh/curl) publishes with the landing check switched off. The occasion observes a local file under .git/refs/remotes, not the remote.
