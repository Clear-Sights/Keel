# L2 CONFIRMED [loader/break] plugin/keel/clauses.py _admit() (subject only reaches CLAUSE-FIXTURE-POS-UNKEYABLE, l.667-678); plugin/keel/dispatch.py _subject() and pre_tool_use l.414-419 (`if isinstance(cl.subject, dict) and not subject: continue`)

## claim
An occasion must be name-agnostic: it reads the host tool enum or an observed effect, never a program's name, so an act spelled under another name cannot proceed unguarded (Theorem 3, Theorem 5).

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack
python3 $S/REPRO.py           # section [L3]: admission + class
bash $S/e2e.sh $S/z03.json "git push origin main" "gitp push origin main" "hub push origin main" "rm -rf /"
# clause Z03: fingerprint {"kind":"regex","on":"tool_name","pattern":"^Bash$"}   (agnostic)
#             subject     {"on":"tool_input.command",
#                          "pattern":"\\b(?:git|/usr/bin/git)\\s+(push|commit)\\b","group":1}

## observed
ADMITTED, `fp=tool-enum/host` -- the loader's own words for the occasion are "reads tool_name, a closed host enum: covered however the shell is spelled". Through the hook: `git push origin main` -> deny ("[Z03] NOMINAL SUBJECT FIRED -- keyed on `push`"); `gitp push origin main` -> {}, `hub push origin main` -> {}, `rm -rf /` -> {}. The clause fires only on the spelling in the subject regex, and the deny text hands the spelling back to the agent.
