# DL-13 CONFIRMED [dispatch/weakness] dispatch.HANDLERS `HANDLERS.get(event.get("hook_event_name"), pre_tool_use)`; pre_tool_use's guard branch `if cl.event not in ("Stop","SubagentStop") and C.discharges(cl, event)`

## claim
dispatch.py docstring: "The lookup's default is the wildcard: an event with no specialist row is still held to the clause table, so newly wired events cannot silently bypass evaluation."

## reproducer
cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad && P=$PWD/attack/dispatch/plugin && ST=$PWD/stE && rm -rf $ST
h(){ printf '%s' "$1" | KEEL_STATE_DIR=$ST CLAUDE_PLUGIN_ROOT=$P bash $P/hooks/dispatch.sh; echo; }
h '{"session_id":"e","hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"git push"}}'
h '{"session_id":"e","hook_event_name":"TotallyMadeUpEvent","tool_name":"Glob","tool_input":{"pattern":"*"}}'
h '{"session_id":"e","hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"git push"}}'
h '{"session_id":"x","hook_event_name":"Notification","tool_name":"Bash","tool_input":{"command":"rm -rf /"}}'

## observed
Fresh session: Bash -> deny [A01][A02][A03]. An event named `TotallyMadeUpEvent` carrying tool_name Glob -> `{}`, and it writes a discharge row. Next Bash -> deny naming only ['A01','A03']: A02 was paid by an event whose name keel has never heard of. An unknown `Notification` event carrying `rm -rf /` -> `{}` allowed (no clause declares that event, so nothing can match).
The wildcard default is therefore asymmetric: an unrecognised event can never raise a demand but can always discharge one. It is not reachable through the shipped hooks.json today (every registered event has a specialist handler), so this is latent -- but it means "newly wired events cannot silently bypass evaluation" is exactly backwards for the discharge direction: a newly wired event is a free guard.
