# L7 CONFIRMED [loader/weakness] plugin/keel/clauses.py _fixture_event() (l.449-469), _admit fixture loops (l.650-712), classify_side `nonzero -> positive` (l.401) and `always` in AGNOSTIC_CLASSES (l.410)

## claim
'A fixture the clause cannot key is not evidence that the clause covers it' -- the fixture laws are what stop a clause being admitted as evidence of coverage it does not have; class `positive` compares a datum the trace produced to one the report states (Thm 6, 7); class `always` is the terminal Theorem-3 boundary.

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack
python3 $S/REPRO.py    # sections [L4], [L5], [L6]
# L4  fingerprint {"kind":"nonzero","on":"tool_input.command"}, fixtures_pos ["7"], fixtures_neg ["0"]
# L5  discharged_by {"kind":"always","tools":["Read"]}, no-discharge fixture dodged via the tools filter
# L6  discharged_by {"kind":"always","event":"PostToolUse","tools":["Bash"],
#                    "any_of":[{"kind":"effect","effect":"report_paths"}]}
# plus: clause tools=["Bash"] with fingerprint regex ^Read$ on tool_name (id Z13) -- also ADMITTED

## observed
L4 ADMITTED as `positive/datum`; fixtures "7"/"0" pass because _fixture_event writes them into tool_input.command, a field no real Bash event ever fills with a number -- `match()` on 'rm -rf /' is False, so the clause is dead in production while the census counts it as a Theorem-6/7 covering. L5 ADMITTED as `always/always`: the guard is spent by a Read of /etc/hostname (discharges -> True) -- a guard that observes nothing. L6 ADMITTED as `effect/world` -- the loader and proofs both say 'the observer measures it' -- and it is discharged by `echo hi` on an event whose entire effect record is False (discharges -> True). Z13 (clause tools=[Bash], occasion requires tool_name=Read) also ADMITTED and can never fire.
