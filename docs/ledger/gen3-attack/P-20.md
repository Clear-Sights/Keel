# P-20 NOVERDICT [points/break] U20 (and every PostToolUse-effect clause via `_open_effect_denial`)

## claim
README: 'Without the line, a Bash call under an open demand is refused' — and 'denial is the resting state'. The commitment is 'not a bypass' because 'after it ran its effect record is what pays'.

## reproducer
bash /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work/setup.sh && cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && python3 repro.py marker

## observed
`rm -rf d1` raised U20. A plain `rm -rf d2` was then denied ['U20'] and d2 survived. The same command under a `# keel-guard: U20` header was ALLOWED and d2 was destroyed ('d2 destroyed anyway: True'); the journal recorded stage 'broken_commitment'. The marker is checked at PreToolUse against the clause id only — never against what the call is — so naming the open demand you have no intention of paying executes any destructive Bash act. The demand staying open afterwards does not restore d2.
