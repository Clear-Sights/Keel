# P-18 CONFIRMED [points/break] U10

## claim
Guard: 'look at the structure first: a query ON THE SAME FILE that prints a non-null JSON datum, or Read the file'. dispatch.py: 'A dict subject with no match yields "" and the caller treats that as unkeyable; an empty key would silently merge every demand for the clause'.

## reproducer
bash /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work/setup.sh && cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && python3 repro.py u10

## observed
`cat payload.json | jq -r '.missing'` printed 'null' and raised demand U10 with subject '' (the subject pattern requires the .json path at END of the command, which a pipeline defeats). A host Read of /…/repo/f.txt — a different, non-JSON file — then discharged it (next PRE Bash -> ALLOW, Stop -> ALLOW). post_tool_use has no equivalent of pre_tool_use's unkeyable-subject abstention, so every unkeyable U10/U02 demand merges into one '' bucket that any equally unkeyable guard event pays.
