# L3 CONFIRMED [loader/break] plugin/keel/clauses.py _compile() key_from block (l.499-515, CLAUSE-KEY-FROM-INVALID) and event_key() (l.134-164); shipped clause C08-check-can-fail, whose key_from pattern hard-codes `python[0-9.]*\s+(?:-m\s+)?`

## claim
No side selects on a program's name; a guard is a host tool call or an observed effect of the guard act, never a spelling (CLAUSE-GUARD-NOMINAL). Theorem 5: an unlisted spelling is a miss, and the loader refuses that shape.

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack
# (a) the SHIPPED table, real hook, three sessions:
python3 $S/c08c.py
#     A: PostToolUse `npm test` report_pass ; Stop
#     B: + PostToolUse `npm run lint` report_fail ; Stop
#     C: + PostToolUse `sh -c 'npm test --plant-fault'` report_fail ; Stop
# (b) a planted key_from that is a literal allowlist of checker names (id Z12):
python3 - <<'EOF'   # full script embedded in the transcript; plants Z12 as the ONLY clause and drives the shim
EOF
# summary of (b): PostToolUse report_pass for `pytest -q`, `./run-tests.sh`, `uv run pytest -q`, each followed by Stop

## observed
(a) A (control): Stop blocks naming [C08-check-can-fail]. B: Stop does NOT name C08 -- a DIFFERENT checker (`npm run lint`) failing discharged the demand that `npm test` passing raised, because both key to the token "npm": fail-open, the pass is licensed by a fault nobody planted in it. C: Stop still names C08 -- the SAME checker proved fallible as `sh -c 'npm test --plant-fault'` keys to "sh" and cannot discharge: fail-closed on the correct act. (b) ADMITTED. event_key for `pytest -q` = 'pytest'; for `./run-tests.sh` = '' . Through the hook: `pytest -q` PASS -> Stop blocks [Z12]; `./run-tests.sh` PASS -> Stop = {} (no obligation ever raised); `uv run pytest -q` PASS -> Stop blocks. An unlisted spelling of the same act is a total miss, admitted by the loader.
