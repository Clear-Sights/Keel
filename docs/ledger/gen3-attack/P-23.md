# P-23 NOVERDICT [points/weakness] A01, A02, A03 (tools:[Bash]) vs Task / ExitPlanMode / AskUserQuestion; P02's guard

## claim
dispatch._applicable gates each clause by its declared `tools`; the deny reason names only what that clause guards.

## reproducer
cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && bash setup.sh && python3 - <<'EOF'
import sys,re; sys.path.insert(0,'.')
from repro import K
k=K('leak'); k.q('SessionStart',hook_event_name='SessionStart')
k.q('PRE Bash rm -rf build/',hook_event_name='PreToolUse',tool_name='Bash',tool_input={'command':'rm -rf build/'})
k.q('PRE Task',hook_event_name='PreToolUse',tool_name='Task',tool_input={'description':'x'})
k.q('PRE ExitPlanMode',hook_event_name='PreToolUse',tool_name='ExitPlanMode',tool_input={'plan':'x'})
k.q('PRE AskUserQuestion (P02 guard)',hook_event_name='PreToolUse',tool_name='AskUserQuestion',tool_input={'questions':[{'question':'q'}]})
EOF

## observed
A denied `rm -rf build/` recorded A01/A02/A03 demands; every subsequent non-host-read tool call was then refused under those same demands — PRE Task -> ['A01','A03'], PRE ExitPlanMode -> ['A01','A03'], and PRE AskUserQuestion -> ['A01','A03']. AskUserQuestion is P02's only guard, so a session that trips A01/A03 first cannot ask the question that would discharge P02 until it reads Keel's own state files; and clauses declaring tools:[Bash] end up refusing planning and delegation, which their occasions say nothing about. Only Read/Grep/Glob are exempted (HOST_READS), which is exactly the set that P-01 shows pays half the table.
