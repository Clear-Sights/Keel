# EV-04 CONFIRMED [evidence/break] README.md:286-287 "Two clauses[^m-local-tooling-clauses] (`U01`, `U02`) name Keel's own probe in their guards; the loader refuses the table if that file is not in the bundle."

## claim
Two things: (a) the clauses matched by the footnoted `local-tooling-clauses` command are U01 and U02; (b) `keel.clauses.load_default()` refuses the table when `plugin/tools/probe_child_capability.py` is absent.

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack; cp -r /home/user/Keel $S/probegone; cd $S/probegone
python3 -c "import json,re; r=json.load(open('plugin/keel/clauses.json')); print([x['id'] for x in r if re.search(r'tools/|probe_child_capability|scanner', x['guard']+str(x['fingerprint']))])"
rm plugin/tools/probe_child_capability.py
PYTHONPATH=plugin python3 -c "from keel import clauses as C; print('loaded', len(C.load_default()), 'clauses -- the loader did NOT refuse')"
grep -rn 'probe_child_capability' plugin/keel/*.py || echo 'no loader reference at all'

## observed
(a) the footnoted command matches `['U01', 'U25']`, not U01/U02 -- U02's guard is "after a failure, change something, then run the target's probe and see it report PASS" and names no repository-local tool; U25 matches only on the word "scanner". (b) with the file deleted the loader prints `loaded 24 clauses -- the loader did NOT refuse`, and `grep` finds no reference to `probe_child_capability` anywhere in `plugin/keel/*.py`. Both halves of the sentence are false and no check binds either.
