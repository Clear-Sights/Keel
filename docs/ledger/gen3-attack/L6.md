# L6 CONFIRMED [loader/weakness] plugin/keel/clauses.py: CLAUSE-PROBE-MUTATING (l.534-535), CLAUSE-NO-FIXTURES (l.613-614), CLAUSE-GUARD-FIXTURE-HIT (l.709-712), CLAUSE-ID-DUPLICATE (l.748-749), CLAUSE-KEY-FROM-INVALID (l.507-508), CLAUSE-SIDE-UNCLASSIFIED (l.579-583)

## claim
The admissibility rules live in the loader because 'a table that cannot state why it reads raw text is a table nobody should be able to ship', and the suite is the fence that keeps them honest (every plant site is a green-then-red pair).

## reproducer
S=/tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack
bash $S/muts.sh          # copies /home/user/Keel six times, neuters one raise in each, runs `python3 -m pytest tests -q`
# the three that showed failures under 6-way parallel load were re-run serially:
for m in PROBE_MUTATING NO_FIXTURES KEY_FROM_INVALID; do (cd $S/mut_$m && PYTHONPATH=plugin python3 -m pytest tests -q | tail -3); done
# census of which codes any test file even mentions:
for c in $(grep -o 'CLAUSE-[A-Z-]*' /home/user/Keel/plugin/keel/clauses.py | sort -u); do echo "$c : $(grep -rl $c /home/user/Keel/tests --include='*.py' | tr '\n' ' ')"; done
# consequence of the untested one:
cd $S/mut_PROBE_MUTATING && python3 -c "...plant a clause with probe cmd ['sh','-c','touch /tmp/keel_pwned']..."

## observed
All six mutations: `246 passed, 1 warning, 122 subtests passed` -- exit 0, full suite green with the check gone (the three that failed in the parallel batch were load flakes in test_effects/test_measured; serial re-runs are 246 passed). 11 of the 23 refusal codes are named in no test file at all: ACTIVATION-FIXTURE-MISS, BUNDLE-INVALID, GUARD-FIXTURE-HIT, ID-DUPLICATE, KEY-FROM-INVALID, NO-ACTIVATION-FIXTURES, NO-FIXTURES, NO-GUARD-FIXTURES, PROBE-INVALID, PROBE-MUTATING, REGEX-INVALID, SIDE-UNCLASSIFIED. CLAUSE-PROBE-MUTATING is the security-critical one: with it deleted, a clause carrying `probe.cmd = ["sh","-c","touch /tmp/keel_pwned"]` loads, `match()` returns True and `/tmp/keel_pwned exists: True` -- the loader executes an attacker-chosen command with os.environ, inside the hook, and nothing in 246 tests notices.
