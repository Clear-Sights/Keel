# P-19 CONFIRMED [points/break] U01, U02, U03, U06, U08, U09, U12, U13, U19, U20, U24 (all eleven effect-fingerprinted clauses)

## claim
README 'Manual bypass': a leading `# keel-allow: <reason>` 'makes PreToolUse return an empty decision before evaluating any clause'. And: 'An effect the observer could not measure ... is NOT-EVALUABLE and the occasion is treated as live.'

## reproducer
bash /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work/setup.sh && cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && python3 repro.py allow

## observed
After the opening reads, the single call `# keel-allow: reviewed with the owner` + `true` — a no-op — produced eleven demands: U01, U02(subject ''), U03, U06, U08, U09, U12, U13, U19, U20, U24. dispatch.pre_tool_use returns {} on the allow marker BEFORE `_effect_record(..., 'before')`, so no snapshot exists; delta() then returns every effect as None and `match` fails closed on all of them. The next Bash call is denied naming all eleven, and Stop blocks. The clause table's own escape hatch is what makes it claim the session created a signed commit, deleted files, killed a process and published a release. One of the eleven (U02, key '') is in the merged unkeyable bucket the code elsewhere says it refuses to create.
