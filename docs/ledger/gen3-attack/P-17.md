# P-17 CONFIRMED [points/break] U02 (subject), C08-check-can-fail (activated_by.key_from)

## claim
'Every occasion and guard must be NAME-AGNOSTIC ... the loader refuses CLAUSE-OCCASION-NOMINAL / CLAUSE-GUARD-NOMINAL' and refuses a textual covering outright (CLAUSE-TEXT-COVERING, Theorem 1).

## reproducer
cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/points/Keel/plugin && python3 -c "import sys,json;sys.path.insert(0,'.');from keel import clauses as C;tbl=C.load_default();u=[c for c in tbl if c.id=='U02'][0];print('U02.subject=',json.dumps(u.subject));print('same predicate classified AS A SIDE:',C.classify_side({'kind':'regex','on':'tool_input.command','pattern':u.subject['pattern']}));c8=[c for c in tbl if c.id.startswith('C08')][0];print('C08.activated_by class:',C.classify_side(c8.activated_by),'key_from.on=',c8.activated_by['key_from']['on'])" && cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && bash setup.sh && python3 repro.py u02key

## observed
U02.subject = {"on":"tool_input.command","pattern":"(?:dispatch\\.sh|--target)\\s+([A-Za-z0-9][\\w.-]*)\\b"} — literal program and flag spellings. The identical predicate offered as a SIDE is classified 'textual', which _admit refuses with CLAUSE-TEXT-COVERING; as a `subject` it is never class-checked (_admit loops only over fingerprint, activated_by, discharged_by). Live consequence: the same re-launch spelled `./dispatch.sh --target alpha` produced demand U02 subject 'alpha'; spelled `./worker.sh alpha` it produced U02 subject '' — the merged, unkeyable bucket. C08's key_from also reads tool_input.command as raw text inside an activated_by that classifies as 'effect', so both keyed clauses are keyed nominally under an agnostic-looking wrapper.
