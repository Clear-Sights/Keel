# P-03 CONFIRMED [points/break] U12 vs U19 (and U13)

## claim
README lists three distinct costly fates: 'apply a patch' (U12), 'apply a generated patch' (U13), 'perform an in-place text rewrite' (U19).

## reproducer
python3 -c "import json,itertools;d={c['id']:c for c in json.load(open('/home/user/Keel/plugin/keel/clauses.json'))};core=lambda c:{k:c[k] for k in ('event','tools','subject','window','fingerprint','discharged_by')};print('U12 core == U19 core:',core(d['U12'])==core(d['U19']));print([ (a,b) for a,b in itertools.combinations(d,2) if core(d[a])==core(d[b]) ])" && cd /tmp/claude-0/-home-user/6821575c-1522-5dd1-b244-faf3d1a3848f/scratchpad/attack/work && bash setup.sh && python3 repro.py u12

## observed
U12 and U19 are byte-identical in every operative field (event, tools, subject, window, fingerprint {kind:effect, effect:files_changed}, discharged_by) — the only pair in the table. Live: one `sed -i` raised exactly three demands U12,U13,U19; one Grep discharged U12 and U19 together leaving only U13; one Read of f.txt then discharged all three. U13's guard set {Read, report_paths} is a strict subset of U12's {Grep, Read, report_paths}, so U13 is U12 with one option removed. Three rows, one distinction the dispatcher can make.
