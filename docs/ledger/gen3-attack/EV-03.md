# EV-03 CONFIRMED [evidence/break] README.md:301-302 "10 sessions[^m-derailments] end at the refusal; 15 continue through the guard"; MEASURED.tsv row `derailments`; eval/corpus/*.jsonl

## claim
README's Evidence section publishes the corpus breakdown 10 + 15 + 1 = 26, footnoting the first number to the `derailments` command in MEASURED.tsv.

## reproducer
cd /home/user/Keel && sed -n '300,303p' README.md && awk -F'\t' '$1=="derailments"{print "MEASURED value:",$2,"of",$3}' MEASURED.tsv && python3 -c 'import glob,json; h=[json.loads(open(p).readline()) for p in glob.glob("eval/corpus/*.jsonl")]; print("fires:",sum(x.get("expect","fires")=="fires" for x in h)); print("recovery:",sum(x.get("expect")=="recovery" for x in h)); print("control:",sum(x.get("expect")=="none" for x in h))'

## observed
README says 10 / 15 / 1. MEASURED.tsv's `derailments` row says 9, and its own command prints 9. Recounting the corpus: fires: 9, recovery: 16, control: 1. Both published figures are wrong; only their sum is right, which is what let them rot unnoticed. `tests/test_measured.py` re-runs every MEASURED row's command but nothing joins MEASURED to the README that cites it, so the suite stays green.
