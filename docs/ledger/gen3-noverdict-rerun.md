| id | verdict | evidence |
|---|---|---|
| DL-01 | FIXED | hooks.json matcher now includes Write/Edit/MultiEdit/NotebookEdit on both PreToolUse and PostToolUse: `PreToolUse matcher: Agent\|AskUserQuestion\|Bash\|Edit\|ExitPlanMode\|Glob\|Grep\|MultiEdit\|NotebookEdit\|Read\|Task\|Write`, `PostToolUse matcher: Bash\|Edit\|MultiEdit\|NotebookEdit\|Read\|Write`. The claimed "matcher never selects file-mutating tools" no longer holds. |
| DL-02 | REPRODUCES | First Bash denied naming [A01][A02][A03]; bare `Glob **/*.nope` PreToolUse -> allow; second Bash denied naming only [A01][A03] — A02 silently paid by the Glob call, identical to observed. |
| DL-03 | REPRODUCES | `rm -f f1.txt` allowed, U20 raised; honest `python3 -m pytest` denied by U20; three further `# keel-guard: U20\nrm -fN.txt` all ALLOWED; final worktree `['f5.txt']`; Stop still blocks on U20. |
| DL-05 | REPRODUCES | Session "A": `rm -f b.txt` raises U20, next Bash denied; Stop under "A" -> block "1 unreconciled obligation(s): [U20]". Session "B" (same state/worktree, after the two artifact Reads): Stop -> `{}` clean. |
| DL-12 | REPRODUCES | 72 pre-paid discharge rows appended by ordinary Bash heredoc -> ALLOWED; effect probe on that write: `files_changed: [] files_removed: [] not_evaluable: None`; subsequent `rm -f a.txt b.txt` and `git push --force` both ALLOWED; Stop -> `{}` clean. |
| DL-14 | REPRODUCES | Refused `git push --force` still writes A01/A02/A03 demand rows; Task, ExitPlanMode and AskUserQuestion (none the refused act) then all denied on those rows; Stop -> block "3 unreconciled obligation(s): [A01]...[A03]..." for a session with only reads and a refused attempt. |
| EFF-01 | REPRODUCES | `rewrite pre: allow owed after: ['U12', 'U13', 'U19']` then `FORGED GUARD 'echo a.txt' pre: allow \| owed now: []` — identical to observed; bare `echo a.txt` alone confirmed via forge_real.py: `'echo a.txt' -> ['named_paths', 'pre_image', 'report_paths']`. |
| EFF-02 | REPRODUCES | forge_real.py: `'echo main' -> ['pre_image', 'report_ref']`; m2.py: `F: 'cat note.txt' (sha in file)-> report_ref=True`. No ref is actually read in either case. |
| EFF-03 | REPRODUCES | m2.py: `C: 'echo <pid> 1' -> report_pids=True report_listing=True report_self=False` is identical to `D: 'ps -eo pid \| head -5' -> report_pids=True report_listing=True report_self=False` — forged listing indistinguishable from the honest one. |
| EFF-04 | REPRODUCES | forge.py cases match observed exactly (`'echo PASS' -> ['report_nowarn','report_pass']`, etc.). hook4.py (after paying the A0x opening debt): `rm f0.txt` allow/U20 raised; `# keel-guard: U20\necho PASS` allow, and U20 is discharged (Stop then blocks only on C08-check-can-fail); `rm f1/f2/f3.txt` all subsequently allowed with no U20 recheck; FINAL files left `['.git', 'f4.txt', 'f5.txt']`. |
| MATH-01 | FIXED | Replacing Coverings.v with a 2-line stub and gutting Clauses.v to comment markers now fails: `COQ=FAIL Coverings.v: compiles and proves nothing (no result in its index)`, EXIT=1 (previously PASS/EXIT=0). |
| MATH-02 | FIXED | Injecting `Hypothesis world_is_convenient : False` + a contradictory theorem now fails: `COQ=FAIL Coverings.v: Section parameters ['world_is_convenient'] are not on the PARAMETERS line...`, EXIT=1 (previously PASS/EXIT=0, axioms=0). |
| MATH-03 | FIXED | All three variants (naive, "declaration not at line start", `#[local]` attribute prefix) now fail identically: `COQ=FAIL Coverings.v: declares global axioms: ['cheat'/'cheat2']`, EXIT=1 (previously two of three evaded as PASS/EXIT=0). |
| MATH-08 | REPRODUCES | Output byte-identical to observed: `report_self('ps aux')=True` vs `report_self('PS aux')=False` (rename flip); textual mentions still discharge (`echo 'the suite would print 12 passed'` -> pass=True); `Theorem 1 test: P(c)=True P(mention c)=True`. |
| P-01 | REPRODUCES | `repro.py grand`: SessionStart + 2 Reads + 1 AskUserQuestion license the whole table — PRE ExitPlanMode/Task/`git push --force`/`rm -rf build/` and Stop all ALLOW. Loader count: single observed.json Read discharges the same 12 clauses `['A01','A02','C03-verify-what-returns','D01','P01','T01','U03','U09','U10','U12','U13','U19']`. |
| P-02 | FIXED | Matcher now includes Write/Edit/MultiEdit/NotebookEdit on both events (see DL-01). `repro.py write`: `PRE Edit -> ['U06', 'U20', 'U24']` (real demands raised) and `Stop -> ['U01','U02','U06','U08','U20','U24']` (blocked), not the previously-observed clean ALLOW with no ledger. |
| P-04 | REPRODUCES | U06/U24 fingerprints identical `{"kind": "effect", "effect": "net_out"}`; `nohup sleep 3 &` (background job) raises both `('U06', "'u03c09'")` and `('U24', "'u03c09'")` (plus U01) together in the demand rows. |
| P-11 | REPRODUCES | Real signed commit raises U08; `# keel-guard: U08` + `echo signature banner` discharges it — next PRE deny list drops U08 to `['U06','U12','U13','U19','U24']`, identical to observed. |
| P-13 | REPRODUCES | `ps aux \| grep sleep` raises C09; `# keel-guard: C09-checker-excludes-self` + `printf '1\n2\n'` discharges it; next PRE and Stop both ALLOW. |
| P-14 | REPRODUCES | Backgrounded `sleep 3` completes, then plain `true` ("an ordinary act") raises U03; `PRE next` denied naming U03 among others. |
| P-20 | REPRODUCES | `rm -rf d1` raises U20; plain `rm -rf d2` denied `['U20']`, d2 survives; same command under `# keel-guard: U20` header -> ALLOWED, `d2 destroyed anyway: True`. |
| P-21 | REPRODUCES | `rm -rf build/` denied `['A01','A02','A03']`; a Glob returning 0 results discharges A02 — retry denied only `['A01','A03']`. remote.json for a no-remote repo: `"tips": {}` (empty dict), matching effects.py's `_artifact_read` acceptance of any dict. |
| P-23 | REPRODUCES | Denied `rm -rf build/` writes A01/A02/A03 demand rows; subsequent PRE Task, PRE ExitPlanMode, and PRE AskUserQuestion (P02's only guard) are all then denied on those same unrelated demands. |
| P-24 | REPRODUCES | Single host Read of any file discharges `['C03-verify-what-returns','D01','P01','U10','U12','U13','U19']`; `repro.py c03`: Read observed.json -> Task dispatch -> Stop, result never inspected, still all ALLOW. |
| P-25 | REPRODUCES | `nohup sleep 3 &` demand rows include both `('U06', "'u03c09'")` and `('U24', "'u03c09'")` raised together as ordinary demands, not surfaced as NOT-EVALUABLE. |
| P-26 | REPRODUCES | T02.discharged_by is still `{"kind": "effect", "effect": "remote_landed"}`; named push and push-by-URL both run with `demand rows: []` and Stop ALLOW — no guard call discharges T02, Keel computes it itself. |
