
===== stale  (42) =====
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] If the stale count bothers you, the only fix is a fresh run id, which would re-run the hunts instead of reusing their cached results.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] If the stale count bothers you, the only fix is a fresh run id, which would re-run the hunts instead of reusing their cached results.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Fixing the generator docstring's stale "one block per side" wording, then committing and pushing Batch 1 to #36.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] The fix works correctly when tested directly—the old harness was just pointing at a stale copy.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] All five enumerations are complete (229 math rows, 770 code rows), and matrix.py now shows 100% coverage, so that stale row is closed.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] - **Discontinuity**: the `matrix.py` row was stale; coverage is 100% under the full suite.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] If the stale count bothers you, the only fix is a fresh run id, which would re-run the hunts instead of reusing their cached results.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] - `patch.D.diff` (16579 bytes) is **stale** — exported from the wrong base.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] But minutes returned org-wide tonight, so that verdict is stale.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] That is not stale exhaustion — it reproduces today.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] - `patch.D.diff` is **stale** (wrong base) — do not apply.
[agent-a0ed27ae3f274e5ea.jsonl] The README's documented plothole output is stale (claims 2 failures, actual is 5).
[agent-a1d10d64959bcd48e.jsonl] The authoritative guard in `clauses.json` is `Read observed.json`; the prose line is stale against it.
[agent-a1fc62e6137384112.jsonl] - `/home/user/Small-Tools/BRANCHES.md` — branch inventory; `claude/cleanup-small-tools` @ `57cd851` flagged UNMERGED-STALE, PROPOSED-DELETE pending human decision.
[agent-a324d1bd9cffe42b7.jsonl] - `:111-114` — "Each clause row's `construction` field anchors into `POINTS.md`, so every negative is followed by its positive as a schema property the loader checks, not a cross-document convention." (Slightly stale: `_admit` no longer checks it; `tests/test_fence.py` does — see `clauses.py:1063-1067`.)
[agent-a4429f0c34e22a4f9.jsonl] *The plant discipline that does exist* is aimed at the **fence, not the clauses**: `.github/workflows/ci.yml` step "The fence must be able to fail" runs 24 named `plant "..."` mutations and requires the suite go red for each, plus "A stale rendering must be loud" and "A missing input must be loud" (every file in `$PAGES` removed one at a time, must redden).
[agent-a5c1124660d16ba5b.jsonl] - `8a3f13b6` — `ward/HANDOFF.md`'s "VERIFIED … byte- and mode-identical" block, one of whose six recorded sha prefixes had already gone stale against the subtree it described
[agent-a5c1124660d16ba5b.jsonl] - `0a151804` — `open = required - uncovered - stated` was a subtraction, not a membership check: two stale register lines read as `open: -2`
[agent-a5c1124660d16ba5b.jsonl] - `da2a6ea8` — 82 vendored units condemned in code the repository may not edit; per-unit exemptions were the wrong instrument because their digests go stale on every lawful refresh **by design**
[agent-a5ef3a336c93ff524.jsonl] The only mention of `nonzero` in `clauses.json` is stale prose in C08's `_fixture_note` (clauses.json:372): "The guard reads tool_response.exit_code under kind=nonzero -- read that off `discharged_by`, not off this sentence." — `discharged_by` says otherwise (§2).
[agent-a7afe76d535310ee6.jsonl] `OVERLAPS.tsv` has exactly 3 data rows (A01/A03, U12/U13, U01/U02) — the module docstring's "four relations" and "The A02/U20 row" are **stale**; no A02/U20 row exists in the file.
[agent-a7afe76d535310ee6.jsonl] **"A stale rendering must be loud"** — backs up `docs/img`, resizes `keel-hero.svg`'s viewBox and then deletes `ledger-flow-dark.png`, requiring `python3 -m unittest tests.test_fence.RenderedImagesTrackTheirSource` to go red each time, restoring between.
[agent-a7afe76d535310ee6.jsonl] **Note on scope:** the ci.yml header comment says only the six dev-suite modules that gate the shipped artifact are present plus `test_fence.py`, but `tests/` actually contains **16** test modules — the comment is stale about the count.
[agent-a85715cb06d5b121e.jsonl] - `python3 repo/render_readme.py --check` was stale after the edits; ran `--write` then `--check` → current.
[agent-a8c95bdca2dcddb24.jsonl] | `docs/img/keel-hero.svg` | `tests/test_fence.py:615` (globs `docs/img/*.svg`); `tools/render_readme_images.py:16`; `.github/workflows/ci.yml` "A stale rendering must be loud" (resizes its viewBox as a plant) | image |
[agent-a8c95bdca2dcddb24.jsonl] plant "shared anchor goes stale"    "jq 'map(if .id == \"P02\" then .construction = \"POINTS.md#p02\" else .
[agent-a8c95bdca2dcddb24.jsonl] **A stale rendering must be loud** (env `IMAGES: docs/img`)
[agent-a8c95bdca2dcddb24.jsonl] echo "  source resized, rendering stale -> red"
[agent-a9921a7475d014a73.jsonl] | 77 triggers → 47 combinations → 25 clauses | `Firewood/PLAN.md:786-788` | "The basis is a moving, under-resolved target — do not treat it as settled." Recomputed from stale `71 → 44 → 23` |
[agent-a9921a7475d014a73.jsonl] | 71 → 44 → 23 | same passage | marked "stale for two passes" |
[agent-adec33975b118e3ae.jsonl] - C-CAR-007/008 (ledger.py legacy_state reporting of a stale GYROSCOPE_STATE_DIR/sibling dir) — pure migration-UX convenience with no law asking for it; a one-time rename shim that could be dropped once migration is complete.
[agent-affe4c5bde581bdc4.jsonl] A suite imported `ward` from a *different checkout* via a stale editable-install `.pth`: neutering a check in this tree left "Ran 113 tests OK", and every green figure the repository had ever reported was a claim about other bytes.
[agent-affe4c5bde581bdc4.jsonl] "Red with the fault" is satisfied by a target that is red *always* — already broken, or carrying an expected string that went stale when the code moved underneath it.
[agent-affe4c5bde581bdc4.jsonl] `SKILL.md` had two writers reading two different registers, neither naming the other, and regenerating with one left the other's view stale for the length of a commit.
[agent-affe4c5bde581bdc4.jsonl] A README quoted `./gates.sh` output stale five ways, captured from a run where the proof test still passed.
[agent-affe4c5bde581bdc4.jsonl] `4df5889` records the sharp corollary: the stamp must be a *digest of the inputs*, never a clock — a timestamp makes every regeneration differ and still cannot answer whether the artifact is stale.
[agent-affe4c5bde581bdc4.jsonl] A probe cap and a claim window each sat above a hook timeout *by luck*, with the relation stated only in prose in a third file naming neither constant — lowering the timeout would have left the cap stale with every test green.
[agent-affe4c5bde581bdc4.jsonl] A stale derived cache was read where the source was the subject
[agent-affe4c5bde581bdc4.jsonl] A stale `.pyc` also inflated the mutation counts in the bench.
[agent-affe4c5bde581bdc4.jsonl] - `7a56606` — Correct the mutation counts, which a stale .pyc had inflated
[agent-a1d10d64959bcd48e.jsonl] The authoritative guard in `clauses.json` is `Read observed.json`; the prose line is stale against it.
[agent-a0ed27ae3f274e5ea.jsonl] The README's documented plothole output is stale (claims 2 failures, actual is 5).

===== silently  (23) =====
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] The fix is the module's own rule applied consistently: it already falls open on an undecodable history row, with a comment saying that dropping it silently would push the emptiness branch toward block.
[agent-a06beda03f87786d5.jsonl] The dedup key is the `keel_effect` JSON but the stored value is the **whole fixture** (`setdefault`) — so collapsing keys can silently drop a fixture's event surface from the corpus.
[agent-a1e6acf2ee9fbd801.jsonl] Docstring: *"a map that silently skips a section reports a smaller corpus rather than an error."* Skips when `$GYROSCOPE_CORPUS_DIR` is unset.
[agent-a1fc62e6137384112.jsonl] Those copies are the ones that can silently drift; `asymmetry-plan.md:499` (D5) specifies a byte-compare dual-presence comparator in the standing Small-Tools gate to catch it — **not present in `/home/user/Small-Tools/repo/gate.py` today.**
[agent-a2a19d1a447944e56.jsonl] `C-CLS-077` (_discriminator terminal-clause special case) — near M-LAW-018: silently reroutes fixture grading for `always`-fingerprint clauses, a carve-out the fixture-positive law doesn't state.
[agent-a49c68b4fd9550b2c.jsonl] # emission_conflicts, so silently replacing the incumbent would discard candidates.
[agent-a5c1124660d16ba5b.jsonl] - `dba2abaf` — `setattr` on a name that does not exist succeeds silently: the matrix row read "carries no load" while disarming nothing
[agent-a5c1124660d16ba5b.jsonl] - `69e4c39d` — LEDGER.tsv pinned a commit 17 commits behind; editing its declared denominator to **999** passed both gates silently
[agent-a5c1124660d16ba5b.jsonl] - `7373e4e8` — two workers OOM-killed at ~4.5GB each, "pool silently repopulated, run wedged"
[agent-a5c1124660d16ba5b.jsonl] - `9ccaa1c0` — "Stop codex-herd answering 'did it land?' with the worker's own claim": twenty-one dispatched units each wrote a DONE marker and each landed nothing, because their sandbox made `.git` read-only and every commit failed silently
[agent-a5c1124660d16ba5b.jsonl] BUG B: `apply_doomed()` checked whether a span was *contained by* another condemned span but never whether it *contains* a live one — deleting an outer function silently deleted a live framework-registered nested one.
[agent-a7afe76d535310ee6.jsonl] I ran it: **17 of 24 admitted, 7 silently dropped.**
[agent-a85715cb06d5b121e.jsonl] - Fixed the PROOF read-back (lines ~162-169): distinguished "asked the remote and it differs" (MISMATCH, exit 5) from "could not reach the remote at all" (now `PROOF-NOT-EVALUABLE`, exit 2) — previously a pipeline exit-status bug (`git ls-remote | cut -f1`) meant an unreachable remote was silently scored as MISMATCH.
[agent-a9921a7475d014a73.jsonl] **Deferred, explicitly not abandoned** — `/home/user/Causality/docs/FRONTIER.md:626`: "**Decision: the 59 are DEFERRED, not abandoned, and not silently parked.**"
[agent-aad21ff6e4aa77850.jsonl] -> ..." diagnostic; non-local targets are skipped silently; the returned citation list is the full census regardless of diagnostics	main()	test_citation_check.py::CitationCheckTests::test_planted_broken_markdown_citation_fails_on_a_copy (via CLI: DANGLING emitted)
[agent-ab711ec9b613df1ff.jsonl] Two caveats on that gate: the regex only recognises `Theorem|Lemma|Corollary`, so a result stated as `Fact`, `Remark`, `Proposition`, or `Example` would be silently ungraded; and `Definition`s/`Record`s/the `Inductive violates` are never checked for consistency with anything.
[agent-aed3612c821b68cc1.jsonl] Things I could not cleanly classify as a single decision (flagged rather than silently split or merged):
[agent-affe4c5bde581bdc4.jsonl] bodies plus the constants they read) is a judgement, and a wrong boundary makes the fence silently inert.
[agent-affe4c5bde581bdc4.jsonl] **Unasserted property:** *A recognizer's accepted set was enumerated from the space of forms its subject can legally take, not from the instances its author had in hand; every form outside it is reported, not silently dropped.*
[agent-affe4c5bde581bdc4.jsonl] A surrogate scrub was not injective on dict keys: two differently damaged keys collapsed onto one `U+FFFD` and the earlier one's value was dropped silently, in the module whose one promise is that repair is on the record.
[agent-affe4c5bde581bdc4.jsonl] A hard-coded `/root/.claude/uploads` published one machine's private-data path and made every corpus-backed check silently NOT-EVALUABLE elsewhere.
[agent-affe4c5bde581bdc4.jsonl] A hooks manifest with the event map at top level failed the host's loader outright; every hook the repository generated had been silently absent for the plugin's entire life, and no gate could see it because nothing had ever run the host loader.
[agent-a06beda03f87786d5.jsonl] The dedup key is the `keel_effect` JSON but the stored value is the **whole fixture** (`setdefault`) — so collapsing keys can silently drop a fixture's event surface from the corpus.

===== corrected  (10) =====
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] - **Worktree base defect, corrected.** `isolation: worktree` does not inherit the session HEAD: all four builder worktrees were created at `4f50fcd` (Keel main) while the branch was at `ba072ce`.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Builder D reported "four pre-existing failures" that were only Batch 2 missing from its tree — corrected; the failures were not real.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] - **Worktree base defect, corrected.** `isolation: worktree` does not inherit session HEAD — all four builder worktrees were made at `4f50fcd` while the branch was `ba072ce`.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Builder D's "four pre-existing failures" were only Batch 2 missing from its tree; corrected, they were not real.
[agent-a173efb0e2f153cd2.jsonl] **README.md lines changed** (6 sharpenings, all narrowed-to-instantiated or corrected, never softened):
[agent-a173efb0e2f153cd2.jsonl] - :294-297 U25 discharge corrected from a fictitious `prefix`/`distractor` text condition to the actual shipped `{kind: effect, effect: report_fail}`.
[agent-a173efb0e2f153cd2.jsonl] - :86-92 and :159-163 "detects" corrected to present tense honesty: `verify_chain` has no production caller (`grep` confirmed), only `tests/test_ledger_growth.py`; reworded to "checker the suite runs...
[agent-a7ca1abfef7e06835.jsonl] C-EFF-131 DECIDE — code unchanged; EFFECTS text corrected to state the wider, ungated firing of `report_null`
[agent-a8bc47f11bb676061.jsonl] Corrected rule: an unmatched vocabulary must fall open exactly as a clean exit does at :175.
[agent-ad612bf6c7c8ee272.jsonl] Corrected rule: keep only exact-equality and true path-suffix; the trailing conjunct is dead code.

===== correction  (9) =====
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Keel's push needed the plain form, not `--force-with-lease`: GitHub deletes the remote branch on merge, so the lease has no ref to check — same correction already recorded for Ward.
[agent-a100aa3d092db9c6c.jsonl] There is no DESIGN.md, despite `ops.py:96` referencing "DESIGN.md Sec.1 #2" and `content_store.py:541` referencing "DESIGN-atomic-index, correction 2026-07-22" — **those referenced design documents are not in the tree.**
[agent-a1fc62e6137384112.jsonl] Genuinely generic (a `grep -c` on markers lies); nothing in Small-Tools owns "append-only ledger with correction edges".
[agent-a32829a947f24ac2f.jsonl] Fix the docstring statement-fix (subtraction/correction, no test needed for pure prose per rules, but let's still be careful).
[agent-a5c1124660d16ba5b.jsonl] - `8938bbbaeb` — "**CORRECTION — kappa never saw a sense; every mechanism claim above is retracted.**" `_lemma_keys` splits on non-alphanumerics, so `bank.n.01` and `bank.n.02` both key to `l:bank`.
[agent-a5c1124660d16ba5b.jsonl] - `5d73b7b7` — "**CORRECTION 8: level 1 was not closed — parameter variants never enumerated.** Every primitive is pinned at one author-picked constant: ascent depth 1 and full but never 2 or 3, `_MAX_MWE` fixed at 4, pos coarse only, sense Lesk-only, entity steps=1, relation FrameNet-only"
[agent-a9921a7475d014a73.jsonl] **PLAN-AUDIT.md §3 — Mechanical self-correction test: OPEN**
[agent-a9d23889dd58b4cb0.jsonl] **Severity correction.** The dispatcher does exactly what `clauses.json` specifies; nothing in the hook is broken, and fail-closed is preserved (the denial is over-firing, not under-firing).
[agent-a9d23889dd58b4cb0.jsonl] **Severity correction.** The dispatcher does exactly what `clauses.json` specifies; nothing in the hook is broken, and fail-closed is preserved (the denial is over-firing, not under-firing).

===== never ran  (7) =====
[agent-a41be69f0fb59ecf2.jsonl] The presence of A01 and A03 is itself the proof the prelude never ran, since the two Reads discharge exactly those.
[agent-affe4c5bde581bdc4.jsonl] The assertion was entailed by its own construction: arithmetic on values the test itself wrote, a comparison of a set against its own definition, a claim already guaranteed by the precondition that ran in `setUp`, an `isinstance` over a value whose type the previous line fixed, or a bound with no floor (`<= 10` satisfied equally by "deduplicated" and "never ran").
[agent-affe4c5bde581bdc4.jsonl] - `898de01` — Stop counting tests that never ran as tests that passed
[agent-affe4c5bde581bdc4.jsonl] A predicate-error *logger* that raised abandoned every pattern after it, so a later DENY never ran and the catch-all recorded a loud-allow.
[agent-affe4c5bde581bdc4.jsonl] A fail-closed handler wrote its diagnostic to stderr *before* emitting the deny, so with stderr unwritable the deny never ran and the hook exited 1 — fail-open caused by an observability failure.
[agent-a41be69f0fb59ecf2.jsonl] The presence of A01 and A03 is itself the proof the prelude never ran, since the two Reads discharge exactly those.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Since the actual command never ran (Keel blocked it) and the user's instructions didn't ask me to go satisfy Keel's session obligations, I'm not going to chase that on my own initiative — it would be scope creep beyond the explicit, narrow task given.

===== was wrong  (6) =====
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] The counts you saw were real and my first answer was wrong.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] The counts you saw were real and my first answer was wrong.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] The counts you saw were real and my first answer was wrong.
[agent-a9921a7475d014a73.jsonl] > "**I have recorded where strong-skills was wrong throughout and never once where this plan is.** That asymmetry is itself the defect."
[agent-a9921a7475d014a73.jsonl] **The twenty-clause sweep** — `ADMISSION.md:88`: "once when a bad brief cut twenty clauses in a single pass on identical reasoning." Its rule-form appears in `asymmetry-plan.md:74`: "Identical reasons across distinct items is the tell that the brief was wrong, not the items."
[agent-a9921a7475d014a73.jsonl] **That was measured and it was wrong to act on**: of the fifteen dirty worktrees, only six had their substance in the tree.

===== root cause  (6) =====
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Found the root cause: the act refreshes the real index's stat cache, causing a copy of that index to treat a modified-but-unstaged file as still matching its stale blob.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Found the root cause: the act refreshes the real index's stat cache, causing a copy of that index to treat a modified-but-unstaged file as still matching its stale blob.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Found the root cause: the act refreshes the real index's stat cache, causing a copy of that index to treat a modified-but-unstaged file as still matching its stale blob.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Root cause found: three of the four worktrees were created at `4f50fcd`, an ancestor that predates Batch 2, not at `ba072ce`.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Recording the root cause so it does not recur.
[agent-a9921a7475d014a73.jsonl] - Its stated diagnosis of the prior failures, line 536: `**This removes Class A's root cause, not just its symptom**: all four false dones were "a green ledger over a subset nobody declared" because the population was authored (a register), never derivable; a population defined by a PREDICATE is derivable, so 26 stops being a count and becomes a denominator.`

===== vacuous  (6) =====
[agent-a173efb0e2f153cd2.jsonl] - `disj_with_textual_not_mention_immune` (+ `Definition disj`) — (d) a composed covering with a non-vacuous textual disjunct is not mention-immune.
[agent-a8c95bdca2dcddb24.jsonl] | **No** — `_scan`'s separator class is code; the CI plant `"a separator stops seeing newlines"` is what makes this non-vacuous.
[agent-a8c95bdca2dcddb24.jsonl] echo "::error::VACUOUS -- a resized source left its stale rendering green" >&2
[agent-a8c95bdca2dcddb24.jsonl] echo "::error::VACUOUS -- a missing rendering left the check green" >&2
[agent-a9df482d1abec13dd.jsonl] - **Seven theorems the loader relies on and Coverings.v does not state**, chiefly: composition of agnostic coverings (`composed` is licensed, 5 shipped sides), the host-enum class, the `always` class (P=True is formally `textual` and non-vacuous, so Theorem 1 applies to it), the textual-disjunct corollary (and the `all_of` over-refusal), closure, a per-side result, and NOT-EVALUABLE-is-live.
[agent-ab711ec9b613df1ff.jsonl] - `positive_needs_no_vocabulary`: **vacuous.** It states `positive c <-> exists d, claimed c = Some d /\ observed c = Some d`, which is definitionally `positive c <-> positive c`; the proof is `split; intro H; exact H`.

===== never fired  (4) =====
[agent-a28160e210fe7727c.jsonl] Loader accepted the invalid nested regex (CLAUSE-REGEX-INVALID never fired).
[agent-a5c1124660d16ba5b.jsonl] - `cc3cca79` — `06_hourglass_signature` required headings literally named FUNNEL, NECK and MULTIPLY: on any document but the one it was ported from it never fired
[agent-a5ef3a336c93ff524.jsonl] > "AN EFFECT'S GUARD IS 'LOOK AT WHAT YOU JUST DID', and that cannot be done in advance… Otherwise the suite run every session opens with would license the deletion that comes an hour later -- measured: U20 never fired once a `pytest` had run earlier in the session."
[agent-a28160e210fe7727c.jsonl] Loader accepted the invalid nested regex (CLAUSE-REGEX-INVALID never fired).

===== never read  (4) =====
[agent-a5c1124660d16ba5b.jsonl] A VERDICT OVER A POPULATION THE INSTRUMENT NEVER READ
[agent-aa8445b87eaa158c3.jsonl] `grep -rn failed_closed /home/user/Courthouse/plugin/` returns nothing — the field is never read.
[agent-ae61f6ff94185500a.jsonl] - **One genuine REMOVE:** `held` (lines 579-587) is built and never read.
[agent-aa8445b87eaa158c3.jsonl] `grep -rn failed_closed /home/user/Courthouse/plugin/` returns nothing — the field is never read.

===== did not run  (3) =====
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] The README render did not run (the tool takes no `--write`).
[agent-a4ae95135f7b52287.jsonl] **Not done / out of scope**: did not run `tools/check_coq.py` (compiles/writes into `proofs/`, owned by the concurrent engineer) — skipped since `render_coverings.py --check` already proved zero drift, making a Coq recompile unnecessary.
[agent-a4ae95135f7b52287.jsonl] Did not touch `dispatch.py`/`effects.py`/`ledger.py`/`proofs/` per the restriction, and did not run the full suite or commit, per instructions.

===== wrong base  (2) =====
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Dead ends, one line each: forcing `patch.D.diff` from the wrong base; treating a wrong-base suite failure as pre-existing; `--force-with-lease` against a branch GitHub deleted on merge.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] Dead ends, one line each: forcing `patch.D.diff` from the wrong base; treating a wrong-base suite failure as pre-existing; `--force-with-lease` against a branch GitHub deleted on merge; `render_readme_claims.py --check` without `PYTHONPATH=plugin`, which fails with `RuntimeError: unit suite did not produce a passing test count`.

===== false positive  (2) =====
[agent-a1d10d64959bcd48e.jsonl] A construction here can be ignored, and it can be unhelpful; **it carries no admission machinery**, because a deny's false positive costs every turn of every session and a page's costs the moment it is read."* That is the finding's claim stated as a deliberate choice with its reason.
[agent-a1d10d64959bcd48e.jsonl] A construction here can be ignored, and it can be unhelpful; **it carries no admission machinery**, because a deny's false positive costs every turn of every session and a page's costs the moment it is read."* That is the finding's claim stated as a deliberate choice with its reason.

===== i reported  (2) =====
[agent-a5c1124660d16ba5b.jsonl] - `1b2f715c` — "I reported gate.py as exiting 0 with four failures.
[6821575c-1522-5dd1-b244-faf3d1a3848f.jsonl] I completed the task as instructed: ran the command, it was refused by Keel, and I reported REFUSED with the verbatim reason.

===== never reached  (2) =====
[agent-a7afe76d535310ee6.jsonl] Each plant: restore → sha256 the page set → `eval` a mutation → **fail if the sha is unchanged** ("PLANT NEVER REACHED THE FILE") → run `python3 -m unittest discover -s tests` and **fail if it stays green** ("VACUOUS CHECK") → restore → **fail if the restore is not byte-exact**.
[agent-a8c95bdca2dcddb24.jsonl] `plant()` restores, snapshots `sha256sum $PAGES`, `eval`s the mutation, and fails with `PLANT NEVER REACHED THE FILE` if the digest is unchanged, `VACUOUS CHECK` if the suite stays green, `RESTORE NOT BYTE-EXACT` if restore drifts.

===== false block  (2) =====
[agent-a8bc47f11bb676061.jsonl] So `air`, `bun run dev`, `php artisan serve`, `caddy run` — any launcher outside the net — produces a false block.
[agent-a8bc47f11bb676061.jsonl] Separately, a real `gh pr merge` escapes both the 4-name tool set and `_is_git_push_argv` → false block on "I merged it".

===== never called  (2) =====
[agent-aa27a91b78cab3a35.jsonl] Now L8 — the check that is defined and never called.
[agent-aa27a91b78cab3a35.jsonl] Now L8 — the check that is defined and never called.

===== does not reproduce  (2) =====
[agent-afaece18320681e0d.jsonl] Result: the finding's headline does not reproduce.
[agent-afaece18320681e0d.jsonl] Result: the finding's headline does not reproduce.
