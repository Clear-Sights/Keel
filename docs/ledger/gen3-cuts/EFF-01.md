# EFF-01 [dead] EFFECTS["fetch_head_written"], _fetch_head(), snapshot's snap["fetch_head"], delta's fetch_head_written branch, 60 fixture keys in clauses.json, 34 corpus records, tests/test_effects.py::test_a_fetch_is_seen_by_the_ref_store_not_by_its_name
LOC 84 -> 0

## description
I read every non-fixture string in all 24 clauses (occasion, fingerprint, activated_by, discharged_by, prose) and matched it against EFFECTS. Result: 27 of the 29 effects are named by at least one clause; `fetch_head_written` is named by NONE. Nothing in plugin/ reads it either -- the dispatcher only copies the record onto the event. So the observer paid two `git rev-parse --git-path FETCH_HEAD` calls and two sha256 reads of FETCH_HEAD per act to compute a value that could never change a verdict, and 34 corpus records plus 60 clause-fixture lines restated it as `false`. This repo already condemns exactly this shape in tests/test_clause_fields.py ('a field the runtime ignores may exist -- what it may NOT do is become the grounds for an assertion'); test_a_fetch_is_seen_by_the_ref_store_not_by_its_name was such an assertion, so it goes with the effect (rule 3). The one effect left that no clause reads, `head_moved`, is KEPT: tests/test_occasion_algebra.py::test_the_check_can_fail plants it as the widening target for U09's `head_switched` and asserts the algebra answers 'U08 SUBSET U09'; removing the name turns that plant into CLAUSE-EFFECT-UNKNOWN and destroys the fault it demonstrates. It is also the ref channel of delta's `still` predicate.

## diff
--- a/plugin/keel/effects.py
+++ b/plugin/keel/effects.py
@@ EFFECTS
-    "fetch_head_written": "the repository's FETCH_HEAD changed, or a remote-tracking ref moved: a fetch, by whatever program",
@@ the world, in pieces
-def _fetch_head(root: str) -> str | None:
-    located = _git(root, "rev-parse", "--git-path", "FETCH_HEAD")
-    if not located:
-        return None
-    path = pathlib.Path(root, located.strip())
-    try:
-        return hashlib.sha256(path.read_bytes()).hexdigest()
-    except OSError:
-        return ""  # no FETCH_HEAD yet: a fetch will create it
@@ snapshot
-        snap["fetch_head"] = _fetch_head(root)
@@ delta
-    if root:
-        out["fetch_head_written"] = (before.get("fetch_head") is not None
-                                     and _fetch_head(root) != before.get("fetch_head")
-                                     ) or bool(out.get("remote_ref_moved"))
-    else:
-        out["fetch_head_written"] = False
     out["net_read"] = (None if out["net_out"] is None else

--- a/tests/test_effects.py
+++ b/tests/test_effects.py
-    def test_a_fetch_is_seen_by_the_ref_store_not_by_its_name(self) -> None:
-        bare = os.path.join(self.tmp, "remote.git")
-        git(self.repo, "init", "-q", "--bare", bare)
-        git(self.repo, "remote", "add", "origin", bare)
-        git(self.repo, "push", "-q", "origin", "main")
-        self.assertTrue(self.observe("git fetch origin")["fetch_head_written"])
-        self.assertFalse(self.observe("echo 'git fetch origin'")["fetch_head_written"])
-        self.assertFalse(self.observe("git status")["fetch_head_written"])

--- a/eval/generate_corpus.py   (3 sites)
-    act("git fetch origin", sid, net_out=True, fetch_head_written=True)
+    act("git fetch origin", sid, net_out=True)

--- a/plugin/keel/clauses.json : 60 `"fetch_head_written": false|true,` fixture lines deleted
--- a/eval/corpus/*.jsonl      : regenerated by `python3 eval/generate_corpus.py --write` (21 files, 34 records lose the key)

## gate
Ran 244 tests in 196.393s / OK ; REPLAY sessions=26 passed=26 failed=0 ; views match plugin/keel/clauses.json ; proofs/Clauses.v matches plugin/keel/clauses.json ; COQ=PASS Clauses.v covers sides=51 of clauses=24 ; Coverings.v: results=16 axioms=0 ; Clauses.v: results=72 axioms=0 ; eval/corpus matches 26 specs ; git status --porcelain | wc -l = 26 ; chained exit=0

## complexity
Instrumented `subprocess.run` in keel.effects and ran 10 steady-state acts (snapshot + act + delta) on a scratch repo. git subprocesses per act 16.0 -> 14.0 from this cut alone (`git rev-parse --git-path FETCH_HEAD` x2/act removed), plus two sha256 hashes of the FETCH_HEAD file. Differential check: 39 recorded observations (32 acts + observed.json + remote.json + at_stop + read_delta + refs + no-snapshot) produced byte-identical records before and after, the removed key being the ONLY diff line (92 diff lines, 92 of them `fetch_head_written`).

## verdict
{"id": "EFF-01", "refuted": false, "reason": "STANDS. Built in /tmp/.../re-runs/c01 on head 5f5dfa6 (note: /home/user/Keel's worktree is at 8df26db; 5f5dfa6 is the divergent commit the hunter worked from and the only one whose effects.py matches every 'before' snippet in the filings, so I checked it out in my copy).\n\nDEADNESS VERIFIED PROGRAMMATICALLY, not by eye: I stripped every fixtures_* key from all 24 clauses and substring-matched each of the 29 EFFECTS names against the remaining JSON (occasion, fingerprint, activated_by, discharged_by, prose, deny_reason, construction). Exactly two names have no clause hit: fetch_head_written and head_moved. The hunter's count (29 effects, 27 named) is exact. Nothing in plugin/, tools/, proofs/ or README names it either; the dispatcher only copies the record onto the event.\n\nDIRECTION: fetch_head_written was `FETCH_HEAD hash moved OR bool(remote_ref_moved)` -- strictly implied by remote_ref_moved, which T02 does read, so no occasion is lost. Removing the name NARROWS what a clause may say: planting `discharged_by: {kind: effect, effect: fetch_head_written}` on U19 is refused with CLAUSE-EFFECT-UNKNOWN after the cut (base refused it later, at CLAUSE-GUARD-FIXTURE-MISS). Fail-closed, both before and after.\n\nEQUIVALENCE: a 20-act differential (rewrite, remove, checkout, commit, reset, tag, detach, planted report shapes, mkdir+commit, quiet acts) run through base and through the cumulative build on repos built with pinned GIT_*_DATE produced byte-identical records on every field -- files_changed/removed, pre_image, head_moved/switched/reset, commit_signed, remote_ref_moved, report_* including report_paths/report_pids/report_listing, observed.json at SessionStart and at the end, and at_stop -- with `fetch_head_written` the ONLY differing key (20 diff lines, all of them that key).\n\nCOMPLEXITY REAL: instrumented subprocess.run inside keel.effects, 10-15 steady-state acts. Base 16.0 git subprocesses/act (9 snapshot + 7 delta -- the hunter's split is right); c01 14.0/act. The two `git rev-parse --git-path FETCH_HEAD` and two sha256 reads of FETCH_HEAD per act are gone.\n\nLOC REAL: counted non-blank non-comment across the tree, -85 (effects.py 639->622 = -17; tests/test_effects.py 408->400 = -8; clauses.json 5034->4974 = -60). Claimed -84; accurate.\n\nRULE 3 DONE PROPERLY -- and this is the one correction the hunter owes. They DELETED test_a_fetch_is_seen_by_the_ref_store_not_by_its_name. All three of its assertions were on the cut key, so it is a restatement; but the property its NAME asserts (a fetch is seen by the ref store, not by its name -- Theorem 3) survives on remote_ref_moved, and after the deletion NO test drives a real `git fetch` through the live observer (remote_ref_moved is otherwise exercised only by fabricated keel_effect dicts in test_clause_fields.py). I rebuilt it as a DERIVED test in re-runs/c01r:\n\n    def test_a_fetch_is_seen_by_the_ref_store_not_by_its_name(self) -> None:\n       
