/loop until: zero fixable rows open (refuter-upheld), every workflow lane dry against the grown pollinate matrix, every fixed row ticked in code, test and ledger cell, every surviving line of Keel's plugin has a kill-witness, plugin LOC below launch (2205 non-blank non-comment lines at 5f5dfa6), the cleanup loop below has terminated, AND the final test has run.

GOAL: everything subtraction and assignment, agnostic, the smallest ruleset that covers all 24 points. Hypothesis = the 24 clauses under the agnostic law; oracle = the owner's sentences (never a nominal side; all 24 covered; never soften a stated limit; no clause sweeps; no model id in pushed artifacts). Rows the oracle does not decide go to the owner with the sentence they need, never fixed: the commitment marker's design, any foundations item that changes the design, any seat disposition (retire/fold/skill) in Small-Tools. A row rejected by the gate three times with no subtraction found, or one the refuter and a lane keep disputing, is the owner's: report it, stop working it.

EACH TICK: read notifications first. If a workflow finished, read only its returned structured result (never journals or agent prose); write each confirmed break or cut as cells into the ledger on disk (Keel: eval/attacks.jsonl in the pollinate cell format; Small-Tools: repo/pollinate.jsonl); fix by subtraction, re-run the cell to green, propagate the angle to every equivalent target (dropped cells fail the gate). Run the kill-witness sweep on touched files and cut or plant every unwitnessed line. Then launch the next generation (Review, never Owner sign-off) seeded from the open rows and the emptiest matrix cells; a generation with no confirmed finding and no new owner row makes the next one collapse-only (simplify only); two such in a row ends the attack phase. A workflow that hung or returned null lanes: stop it, relaunch. Every generation attacks the process too: one fabricated finding per lane injected into the verify stage must be refuted; a refuter that accepts it is a gate hole, fixed with a failing plant first; every new angle joins the matrix. PRs: Keel #33 and Small-Tools #20 merge on green, branches reset onto main; behaviour fixes land before cuts; version 2.1.0 then release.yml on main. Receipts every tick: every gate green by bare exit code, clean trees, commit, push, verify by ls-remote. Report only changes: accepts with what was removed, owner rows verbatim, rejections, process breaks, plugin LOC and test LOC; noop if nothing moved.

CONTEXT: compact at the end of every generation. Never paste tool output or file contents into replies; cite the path. The ledger on disk (scratchpad/ledger/STATE.md plus the two pollinate ledgers) is the memory; the conversation carries deltas and decisions.

CLEANUP, once the attack phase is done, as its own loop: /loop until a full pass yields no qualifying finding AND no legal simplification. Alternate REVIEW (grid per-invariant across pieces and per-piece across invariants, per-cell coverage stated, findings cite a ledger row plus an executed reproducer or structural proof) and SIMPLIFY (legal only as ledger-preserving transform: old-vs-new passes two_roads.py over eval/corpus; dropped behaviour named relocated or retired; every surviving unit has a kill-witness, no witness → cut or plant). Label every claim exhaustive-finite / structural / machine-checked / merely-tested; shrink theorems to their actual quantifier. A dropped cell or unlabeled claim is a false claim.

FINAL TEST, only after cleanup terminates: predictions written to disk first (per corpus session the expected verdict; per attack cell the expected colour; every expected miss named with the owner sentence that allows it), plants seen failing first, then the full corpus replay, the full attack ledger, and every gate, graded exactly as written, no re-prediction. Report the table, what missed and why, and whether each miss is a clause, an effect, or the theory.

ON COMPACTION: /compact Preserve verbatim: the owner's instructions and corrections in their own words, exact paths, branch names, shas, commands that worked, numbers with units, and error strings. Keep the current done-bar stated as a check that can fail. Keep decisions with their reasons, and every failure with its root cause. Label what is verified by an executed trace vs merely believed. Note background work still running and the command to check it. Put anything blocking on the owner at the top. Drop tool transcripts, file dumps, resolved threads and narration; keep one line per dead end. Where a fact was corrected, keep only the corrected form and say it was corrected.
